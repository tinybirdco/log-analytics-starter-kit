NODE mv
SQL >

    SELECT
        toStartOfHour(event_ts) AS hour,
        countState() AS total
    FROM function_logs
    GROUP BY hour
    ORDER BY hour ASC

TYPE materialized
DATASOURCE function_calls_per_hour
ENGINE "AggregatingMergeTree"
ENGINE_PARTITION_KEY "tuple()"
ENGINE_SORTING_KEY "hour"

